services:
  - type: web
    name: postulaciones-lisbeth
    runtime: node
    plan: free
    buildCommand: npm install
    startCommand: node server.js
    envVars:
      - key: EMAIL_USER
        value: lizste.hidalgo@hotmail.com
      - key: EMAIL_PASS
        sync: false
      - key: PORT
        value: "3000"
      - key: NODE_ENV
        value: production
